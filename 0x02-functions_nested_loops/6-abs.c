#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: unsigned integer
 * Return: the absolute value
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);

	else
		return (-r);
}
